function WelcomeMassage({ onGetPostClick }) {
  return (
    <center>
      <h1>There is no posts</h1>
      {/* <button
        type="button"
        onClick={onGetPostClick}
        className="btn btn-primary"
      >
        Get post frome the srtver
      </button> */}
    </center>
  );
}

export default WelcomeMassage;
